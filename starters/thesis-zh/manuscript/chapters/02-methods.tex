\chapter{方法}
\label{ch:methods}

替换本章。新章放进 \texttt{chapters/}，并在 \texttt{chapters.tex} 里列入。

\begin{formulabox}
\[
  \mathcal{L}(\theta) = \mathbb{E}_{x\sim p}\bigl[\ell(f_\theta(x))\bigr].
\]
\end{formulabox}
