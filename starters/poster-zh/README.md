# 经验谱分布与 Marchenko--Pastur 律

Rose Pine A0 中文海报（`ctexart` + XeLaTeX）。

块高度跟内容走，不用手动填高度。字号按 A0 墙报设定。默认横版。

```bash
just build
```

## 横版 / 竖版

默认 **A0 横版**。竖版：在 `main.tex` 里写成 `\posterlandscapefalse`（`\input{preamble}` 之前）。
