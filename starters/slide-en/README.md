# ++PROJECT-TITLE++

Rose Pine English slides (`beamer` + XeLaTeX, 16:9). Visual language follows the book templates.

## Build

```bash
just build
```

## Layout

- `main.tex` / `preamble.tex`
- `frames/`
- `rosepine/` (snapshot of factory `rosepine/`)
