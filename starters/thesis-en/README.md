# ++PROJECT-TITLE++

Rose Pine English thesis (`book` + pdfLaTeX).

Cover fields (school, degree, supervisor) in `manuscript/main.tex`.
Chapters in `manuscript/chapters/`. Bibliography: `manuscript/refs.bib`.

```bash
just build
# PDF: build/main.pdf
```
