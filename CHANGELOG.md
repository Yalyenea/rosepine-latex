# Changelog

## 2026-08-14

- Paper starter is one layer; `just build` writes `.tmp/` and `build/`.
- `just book` / `paper` / `slide` / `notes` / `handout` / `exercises` / `poster` write to `DEST`.
- `just test`, `just sync-theme`.
